const button = document.getElementById('btn');
// button.addEventListener('click', () => {
//     console.log('button')
// });
const form = document.getElementById('form');
/*
const buttonFun = (e) => {
    console.log('button',e.target);
};
// button.addEventListener('click', buttonFun);
form.addEventListener('submit',(e) => {
    e.preventDefault();
    let user = e.target.name.value;
    const email = e.target.email;
    const code = e.target.password;
    user = user.trim();
    // email.trim();
    // code.trim();
    // console.log(user,email.value,code.value);
    if(user.length > 0 && email.value.length > 0 && code.value.length > 0 ){
        if(user.length < 4 || code.value.length < 4){
            console.log('length should be minimum 8 chars')
        }else{

            console.log('successful')
            form.reset()
        }
    };
})
// form.onsubmit = function(e) {
//     e.preventDefault(); 
//     const user = document.getElementById('name');
//     console.log('success',user.value);
// }

// console.log(document.querySelectorAll('form input').forEach((val) => {
//     console.log(val.value);
// }));
*/

// const box = document.querySelector('.box');
// const pa = document.querySelector('.box p')
// box.addEventListener('mouseover', () => {
//     box.innerHTML += '<p>Thank you</p>'
// });
// box.addEventListener('mouseout', () => {
//     pa.nextElementSibling.innerHTML = '';
// })

// document.addEventListener('scroll',(e) => {
//     console.log(window.scrollY);
// })